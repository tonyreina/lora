defaults:
  - model: phi4_mini
  - training: default
  - data: medical
  - inference: default
  - _self_

# Global settings
mode: train  # Options: train, inference, pipeline, demo
seed: 816
output_dir: ./checkpoints/llama3-lora-med

# Hydra configuration
hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: false